# This Program Calculates the Hypotenuse!

#[VAR] :: Creating the Entry!
a = int(input('Set a value to A: '))
b = int(input('Set a value to B: '))

#[FUNC] :: Calculating the Hypotenuse!

c = (a**2 + b**2) ** 0.5

#[EXEC] :: Spittin on the Screen!
print(c)